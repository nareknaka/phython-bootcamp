name = "Angela"
length = len(name)
print(length)

# The variable name is a string variable that stores the value "Angela".
# The variable length is an integer variable that stores the length of the string variable name.
# The len() function is used to calculate the length of the string variable name.